//pub mod user;

use schema::users;

#[derive(Queryable, Identifiable, Clone)]
#[table_name="users"]
pub struct User {
    pub id: i32,
    first_name: String,
    last_name: String,
    pub phone_number: String, // TODO create a phone number type?
    //    groups: Vec<Group>,
    //    pub state: SmState
}


#[derive(Insertable)]
#[table_name="users"]
pub struct NewUser {
    first_name: String,
    last_name: String,
    phone_number: String
}